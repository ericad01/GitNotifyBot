name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - "README.md"
      - "docs/"
  pull_request:
    branches: [main]
    paths-ignore:
      - "README.md"
      - "docs/"

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check repository
        run: echo "CI pipeline is working correctly!"